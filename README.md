# 🤖 Классификация и анализ текстовых запросов

Проект для автоматической обработки, анализа и классификации текстовых запросов пользователей, связанных с доставкой и операторской поддержкой. Включает полный пайплайн обработки данных от предобработки до машинного обучения с автоматическими ответами на запросы.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Структура проекта](#структура-проекта)
- [Установка](#установка)
- [Использование](#использование)
- [Компоненты проекта](#компоненты-проекта)
- [Примеры](#примеры)

## 📖 Описание

Проект предназначен для:
- **Предобработки** текстовых данных (очистка, нормализация)
- **Анализа** данных (EDA, статистика, визуализация)
- **Векторизации** текста для машинного обучения (TF-IDF)
- **Классификации** запросов по категориям/кластерам
- **Определения типа** запроса пользователя (доставка, оператор, и т.д.)

## ✨ Возможности

- 🚀 **Полный пайплайн за один запуск** - от загрузки датасета до обработки запросов
- 🔍 **Автоматическая классификация запросов** на основе правил и кластеризации
- 💬 **Автоматические ответы на запросы** - система генерирует ответы в зависимости от типа запроса
- 📊 **Исследовательский анализ данных** (EDA) с автоматической генерацией отчетов
- 🎯 **Кластеризация запросов** с помощью K-Means (10 кластеров по умолчанию)
- 📈 **Визуализация** данных и результатов анализа
- 🤖 **Интерактивный режим** для тестирования запросов в реальном времени
- 📝 **Jupyter Notebook** версии всех скриптов для интерактивной работы
- 🎛️ **Гибкая настройка** параметров векторизации и кластеризации

## 📁 Структура проекта

```
leroykknn/
├── dataset.csv                    # Исходный датасет
├── dataset_cleaned.csv            # Очищенные данные
├── dataset_removed.csv            # Удаленные данные
│
├── full_pipeline.ipynb             # ⭐ ПОЛНЫЙ ПАЙПЛАЙН: все операции за один запуск
├── cluster_classifier.ipynb        # Классификатор по кластерам (Jupyter)
├── query_classifier.ipynb         # Классификатор запросов с ответами (Jupyter)
│
├── files/                          # Данные и Jupyter Notebook файлы
│   ├── preprocessing.ipynb        # Предобработка данных
│   ├── eda_report.ipynb           # EDA анализ
│   ├── vectorization.ipynb        # Векторизация текста
│   ├── visualization.ipynb        # Визуализация
│   └── load_vectors_example.ipynb # Примеры работы с векторами
│
│   ├── dataset.csv                # Исходный датасет
│   ├── dataset_cleaned.csv        # Очищенные данные
│   ├── dataset_removed.csv        # Удаленные данные
│   │
│   ├── vectorized/                # Векторизованные данные
│   │   ├── vectorizer_tfidf.pkl   # Обученный векторизатор
│   │   ├── vectors_tfidf_sparse.npz # Векторы (sparse format)
│   │   ├── metadata_tfidf.csv     # Метаданные
│   │   ├── cluster_model.pkl      # Модель кластеризации
│   │   └── cluster_info.csv       # Информация о кластерах
│   │
│   ├── reports/                   # Отчеты
│   │   ├── eda_report.txt         # EDA отчет
│   │   ├── report_cleaned.txt     # Отчет по очистке
│   │   └── vectorization_report_tfidf.txt
│   │
│   └── visualizations/            # Визуализации
│       ├── dimension_reduction_tfidf.png
│       ├── sparsity_tfidf.png
│       └── top_features_tfidf.png
│
├── requirements.txt                # Зависимости
└── README.md                       # Этот файл
```

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/o8c9lnu4/datascienceforenjoyers.git
cd leroykknn
```

### 2. Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Подготовка данных

Поместите файл `dataset.csv` в корневую директорию проекта.

## 💻 Использование

> **💡 Совет:** Если вы только начинаете, используйте `full_pipeline.ipynb` - он выполняет все операции от загрузки датасета до обработки запросов за один запуск!

### Быстрый старт

#### ⭐ Вариант 1: Полный пайплайн за один запуск (РЕКОМЕНДУЕТСЯ)

**Используйте `full_pipeline.ipynb` для максимального удобства:**
- ✅ Все этапы в одном файле
- ✅ Автоматические ответы на запросы
- ✅ Не нужно переключаться между файлами
- ✅ Идеально для экспериментов и демонстраций

Откройте `full_pipeline.ipynb` в Jupyter Notebook и запустите все ячейки по порядку. Этот notebook выполняет:

1. ✅ Загрузку и предобработку данных
2. ✅ Векторизацию текстов (TF-IDF)
3. ✅ Обучение кластерной модели (K-Means)
4. ✅ Интерактивный режим для обработки запросов с автоматическими ответами

**Пример использования:**
```python
# После запуска всех ячеек, введите запрос в интерактивном режиме:
# Запрос: "связь с оператором"
# 
# Результат:
# ============================================================
# РЕЗУЛЬТАТ КЛАССИФИКАЦИИ
# ============================================================
# Тип запроса: operator_request
# Релевантность: [RELEVANT]
# Уверенность: 85.0%
# Детали: Обнаружены ключевые слова: оператор
# 
# 🤖 ОТВЕТ:
#    Ищу свободного оператора для вас...
# ============================================================
```

#### Вариант 2: Отдельные компоненты

##### 1. Запуск полного пайплайна через скрипт

```bash
python run_pipeline.py
```

Это выполнит все этапы обработки:
- Предобработку данных
- EDA анализ
- Векторизацию (опционально)
- Визуализацию (опционально)

##### 2. Классификация запроса по кластерам

**В Jupyter Notebook:**
```python
# Откройте cluster_classifier.ipynb
# Запустите все ячейки
# Измените запрос в последней ячейке:
query = "ПОЛУЧИТЬ КОД"
```

**Результат:**
```
============================================================
РЕЗУЛЬТАТ КЛАССИФИКАЦИИ
============================================================
Запрос: получить код
Кластер: 2
Уверенность: 84.1%

Информация о кластере:
  Размер: 28136 запросов (6.8%)
  Ключевые слова: код, код выдачи, выдачи, получить код, получить
  Примеры: мне нужно получить код заказа | мне нужно получить код | получить код от посылки
============================================================
```

#### 3. Классификация запроса по правилам

**В Jupyter Notebook:**
```python
# Откройте query_classifier.ipynb
# Запустите все ячейки
```

### Параметры запуска пайплайна

```bash
# Показать список доступных шагов
python run_pipeline.py --list

# Запустить только определенные шаги
python run_pipeline.py --steps 1 2

# Пропустить шаги, если выходные файлы уже существуют
python run_pipeline.py --skip-existing

# Пропустить опциональные шаги
python run_pipeline.py --skip-optional
```

## 🔧 Компоненты проекта

### 1. Предобработка данных (`files/preprocessing.ipynb`)

- Очистка текста (удаление спецсимволов, нормализация)
- Классификация релевантности запросов
- Фильтрация неинформативных запросов
- Удаление дубликатов

**Вход:** `dataset.csv`  
**Выход:** `dataset_cleaned.csv`, `dataset_removed.csv`

### 2. EDA анализ (`files/eda_report.ipynb`)

- Статистика по данным
- Анализ частоты слов и фраз
- TF-IDF анализ ключевых терминов
- Автоматическая кластеризация для выявления категорий
- Генерация отчета

**Вход:** `dataset.csv`  
**Выход:** `reports/eda_report.txt`

### 3. Векторизация (`files/vectorization.ipynb`)

- Векторизация текста методом TF-IDF
- Сохранение векторов в разных форматах (sparse, dense)
- Создание метаданных и индексов признаков

**Вход:** `dataset_cleaned.csv`  
**Выход:** Файлы в `vectorized/`

### 4. Визуализация (`files/visualization.ipynb`)

- Визуализация разреженности векторов
- Анализ топ признаков
- Уменьшение размерности (PCA, t-SNE)
- Сохранение графиков

**Вход:** Данные из `vectorized/`  
**Выход:** Графики в `visualizations/`

### 5. Классификатор по кластерам (`cluster_classifier.ipynb`)

- Обучение модели K-Means кластеризации
- Предсказание кластера для нового запроса
- Показ информации о кластере (размер, ключевые слова, примеры)
- Вычисление уверенности предсказания

**Использование:**
```python
# Загрузка данных и модели
vectorizer, vectors, metadata = load_data()
kmeans_model, cluster_info = load_cluster_model()

# Классификация запроса
query = "ПОЛУЧИТЬ КОД"
result = predict_cluster(query, vectorizer, kmeans_model)
print(format_cluster_result(result, cluster_info))
```

### 6. Классификатор по правилам (`query_classifier.ipynb`)

- Классификация на основе ключевых слов
- Определение типа запроса (доставка, оператор, и т.д.)
- Оценка релевантности
- Приоритизация запросов
- **💬 Автоматические ответы на запросы**

**Типы запросов:**
- `delivery_related` - Доставка → *"Проверяю информацию по доставке..."*
- `operator_request` - Оператор → *"Ищу свободного оператора для вас..."*
- `delivery_and_operator` - Доставка + Оператор → *"Ищу оператора для решения вопроса по доставке..."*
- `protected_phrase` - Важный запрос → *"Перевожу на оператора для решения проблемы..."*
- `long_phrase` - Развернутый запрос → *"Анализирую ваш запрос..."*
- `no_relevant_content` - Не релевантный → *"Не совсем понял ваш запрос. Можете уточнить вопрос?"*

### 7. Полный пайплайн (`full_pipeline.ipynb`) ⭐

**Единый notebook для выполнения всех операций:**

1. **Предобработка данных**
   - Очистка текста
   - Классификация релевантности
   - Удаление дубликатов

2. **Векторизация**
   - TF-IDF векторизация
   - Сохранение векторизатора и векторов

3. **Обучение модели**
   - K-Means кластеризация
   - Анализ кластеров
   - Сохранение модели

4. **Обработка запросов**
   - Классификация новых запросов
   - **Автоматическая генерация ответов**
   - Интерактивный режим

**Преимущества:**
- ✅ Все операции в одном месте
- ✅ Не нужно запускать отдельные скрипты
- ✅ Удобно для экспериментов
- ✅ Автоматические ответы на запросы

## 💬 Система автоматических ответов

Проект включает интеллектуальную систему ответов на запросы. В зависимости от типа запроса генерируется соответствующий ответ:

| Тип запроса | Пример ответа |
|------------|---------------|
| `operator_request` | "Ищу свободного оператора для вас..." |
| `delivery_related` | "Проверяю информацию по доставке..." |
| `delivery_and_operator` | "Ищу оператора для решения вопроса по доставке..." |
| `protected_phrase` | "Перевожу на оператора для решения проблемы..." |
| `long_phrase` | "Анализирую ваш запрос..." |
| `too_short` | "Не совсем понял ваш запрос. Можете уточнить?" |

**Примеры ответов можно настроить в словаре `RESPONSE_TEMPLATES` в `full_pipeline.ipynb`.**

## 📊 Примеры

### Пример 1: Полный пайплайн с автоматическим ответом

```python
# В full_pipeline.ipynb после обучения модели:

# Запрос: "связь с оператором"
# Результат:
# ============================================================
# РЕЗУЛЬТАТ КЛАССИФИКАЦИИ
# ============================================================
# Тип запроса: operator_request
# Релевантность: [RELEVANT]
# Уверенность: 85.0%
# Детали: Обнаружены ключевые слова: оператор
# 
# 🤖 ОТВЕТ:
#    Ищу свободного оператора для вас...
# ============================================================
```

### Пример 2: Классификация запроса "ПОЛУЧИТЬ КОД"

```python
query = "ПОЛУЧИТЬ КОД"
result = predict_cluster(query, vectorizer, kmeans_model)
```

**Результат:**
- Кластер: 2
- Уверенность: 84.1%
- Ключевые слова: код, код выдачи, выдачи, получить код

### Пример 2: Классификация запроса "где моя посылка"

```python
query = "где моя посылка"
result = predict_cluster(query, vectorizer, kmeans_model)
```

**Результат:**
- Кластер: 4
- Уверенность: 75.2%
- Ключевые слова: посылка, пришла, моя, не, моя посылка

### Пример 3: Интерактивный режим с ответами

```python
# В query_classifier.ipynb или full_pipeline.ipynb

# Запрос: "где моя посылка"
# Результат:
# ============================================================
# РЕЗУЛЬТАТ КЛАССИФИКАЦИИ
# ============================================================
# Тип запроса: delivery_related
# Релевантность: [RELEVANT]
# Уверенность: 90.0%
# Детали: Обнаружены ключевые слова: доставка
# 
# 🤖 ОТВЕТ:
#    Проверяю информацию по доставке...
# ============================================================
```

### Пример 4: Запуск пайплайна с выбором шагов

```bash
# Только предобработка и EDA
python run_pipeline.py --steps 1 2

# Весь пайплайн без опциональных шагов
python run_pipeline.py --skip-optional
```

## 📦 Зависимости

Основные библиотеки:
- `pandas` - работа с данными
- `numpy` - численные вычисления
- `scikit-learn` - машинное обучение
- `scipy` - научные вычисления
- `matplotlib` - визуализация
- `seaborn` - статистическая визуализация
- `joblib` - сохранение моделей

Полный список в `requirements.txt`

## 📝 Формат данных

### Входной файл `dataset.csv`

Должен содержать столбец с текстовыми запросами. Пример структуры:

| id | text | datetime |
|----|------|----------|
| 1 | получить посылку | 2022-01-25 10:26:08 |
| 2 | где моя посылка | 2022-01-25 11:15:12 |

### Выходные файлы

- `dataset_cleaned.csv` - очищенные и релевантные запросы
- `dataset_removed.csv` - удаленные нерелевантные запросы
- `vectorized/` - векторизованные данные и модели
- `reports/` - текстовые отчеты
- `visualizations/` - графики и визуализации

## 🔍 Типы кластеров

Модель автоматически определяет кластеры на основе данных. Примеры:

- **Кластер 0**: Запросы о заказах и кодах
- **Кластер 1**: Запросы о получении посылок
- **Кластер 2**: Запросы о кодах выдачи
- **Кластер 3**: Запросы о статусе доставки
- И т.д.

## 🛠️ Разработка

### Структура кода

- Все основные скрипты доступны в формате Jupyter Notebook
- **⭐ Рекомендуется использовать `full_pipeline.ipynb` для полного цикла**
- Исходные Python файлы сохранены в `py_files/` (если нужны)
- Главный скрипт `run_pipeline.py` управляет всем пайплайном (если доступен)

### Настройка ответов

Чтобы изменить или добавить ответы на запросы, отредактируйте словарь `RESPONSE_TEMPLATES` в `full_pipeline.ipynb`:

```python
RESPONSE_TEMPLATES = {
    'operator_request': {
        'response': 'Ваш кастомный ответ здесь...',
        'alternatives': [
            'Альтернативный ответ 1',
            'Альтернативный ответ 2'
        ]
    },
    # ... другие типы запросов
}
```

### Добавление новых функций

1. Создайте новый Jupyter Notebook в соответствующей директории
2. Добавьте шаг в `run_pipeline.py` (если нужно)
3. Обновите `full_pipeline.ipynb` для включения новой функциональности
4. Обновите документацию

### Настройка параметров

**Кластеризация:**
```python
N_CLUSTERS = 10  # Измените количество кластеров
RANDOM_STATE = 42
```

**Векторизация:**
```python
vectorizer = TfidfVectorizer(
    max_features=5000,      # Максимальное количество признаков
    ngram_range=(1, 2),     # Униграммы и биграммы
    min_df=2,               # Минимальная частота документа
    max_df=0.95,            # Максимальная частота документа
    # ...
)
```

## 📄 Лицензия

Проект создан в образовательных целях.

## 👥 Авторы

Проект разработан для анализа и классификации текстовых запросов.

## 🎯 Рекомендации по использованию

### Для начинающих

1. **Начните с `full_pipeline.ipynb`** - это самый простой способ запустить весь процесс
2. Запустите все ячейки по порядку
3. Протестируйте интерактивный режим с примерами запросов

### Для продвинутых пользователей

1. Используйте отдельные notebook файлы для экспериментов
2. Настройте параметры векторизации и кластеризации под свои данные
3. Добавьте собственные типы запросов и ответы
4. Расширьте систему ответов для вашего use case

### Производительность

- Для небольших датасетов (< 10K запросов) можно использовать все функции
- Для больших датасетов рекомендуется:
  - Использовать батч-обработку при предобработке
  - Уменьшить `max_features` в векторизаторе
  - Использовать sparse матрицы (по умолчанию)
  - Рассмотреть уменьшение количества кластеров

## 📞 Поддержка и FAQ

### Часто задаваемые вопросы

**Q: Как изменить количество кластеров?**  
A: Измените параметр `N_CLUSTERS` в настройках notebook (по умолчанию 10).

**Q: Как добавить новые ответы на запросы?**  
A: Отредактируйте словарь `RESPONSE_TEMPLATES` в `full_pipeline.ipynb`.

**Q: Где найти обученные модели?**  
A: Все модели сохраняются в `files/vectorized/`.

**Q: Как обработать новый датасет?**  
A: Поместите файл `dataset.csv` в папку `files/` и запустите `full_pipeline.ipynb`.

### При возникновении проблем

1. ✅ Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
2. ✅ Убедитесь, что файл `dataset.csv` находится в папке `files/`
3. ✅ Проверьте логи выполнения в консоли Jupyter
4. ✅ Убедитесь, что используется Python 3.8+

### Известные проблемы

- На Windows могут быть проблемы с кодировкой - проект автоматически настраивает UTF-8
- Для очень больших файлов (> 1GB) может потребоваться больше RAM

---

## 📈 Roadmap

Планируемые улучшения:
- [ ] Поддержка других методов векторизации (Word2Vec, BERT)
- [ ] Веб-интерфейс для обработки запросов
- [ ] API для интеграции с другими системами
- [ ] Расширенная аналитика и метрики
- [ ] Поддержка мультиязычности

---

**Примечание:** Для работы с большими файлами рекомендуется использовать виртуальное окружение и достаточный объем оперативной памяти (минимум 4GB RAM рекомендуется).

**⭐ Быстрый старт:** Откройте `full_pipeline.ipynb` и запустите все ячейки - это самый простой способ начать работу с проектом!

